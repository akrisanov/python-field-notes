[tox]
envlist = py39, py310
skipsdist = true
skip_missing_interpreters = True

[testenv]
deps = pytest
    ; pytest-srcpaths for pytest < 7.x
    -rrequirements.txt
commands = pytest

[pytest]
addopts = -ra
testpaths = tests
pythonpath = src
